#include <stdio.h>

int main(int argc, char** argv) {

    int x = 5;
    int y = 5;
    if (x == y) {
        printf("%d equals %d\n", x, y);
    } else {
        printf("%d does not equal %d\n", x, y);
    }
}
